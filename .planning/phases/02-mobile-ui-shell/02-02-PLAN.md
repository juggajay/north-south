---
phase: 02-mobile-ui-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/navigation/TabBadge.tsx
  - src/components/navigation/BottomNav.tsx
  - src/app/(tabs)/page.tsx
  - src/app/(tabs)/design/page.tsx
  - src/app/(tabs)/orders/page.tsx
autonomous: true

must_haves:
  truths:
    - "Red dot badge appears on Chat tab when unread messages exist"
    - "Number badge appears on Orders tab when order updates exist"
    - "Home tab shows welcome message and camera prompt"
    - "Design tab shows illustration and empty state message"
    - "Orders tab shows empty state with guidance"
    - "All touch targets meet 44x44px minimum"
  artifacts:
    - path: "src/components/navigation/TabBadge.tsx"
      provides: "Badge component for tab indicators"
      exports: ["TabBadge"]
    - path: "src/components/navigation/BottomNav.tsx"
      provides: "Enhanced navigation with badge support"
      contains: "TabBadge"
    - path: "src/app/(tabs)/page.tsx"
      provides: "Home tab with welcome and camera CTA"
      contains: "Take a photo"
    - path: "src/app/(tabs)/design/page.tsx"
      provides: "Design tab empty state"
      contains: "No designs yet"
    - path: "src/app/(tabs)/orders/page.tsx"
      provides: "Orders tab empty state"
      contains: "No orders yet"
  key_links:
    - from: "src/components/navigation/BottomNav.tsx"
      to: "src/components/navigation/TabBadge.tsx"
      via: "component import"
      pattern: "import.*TabBadge"
---

<objective>
Enhance bottom tab navigation with badge indicators and implement proper empty states for all tabs.

Purpose: Establish the visual foundation for the mobile UI shell with clear guidance for new users and notification indicators for returning users.

Output: TabBadge component integrated into BottomNav, plus updated tab pages with the exact empty states defined in CONTEXT.md.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mobile-ui-shell/02-CONTEXT.md
@src/components/navigation/BottomNav.tsx
@src/app/(tabs)/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TabBadge component and update BottomNav</name>
  <files>src/components/navigation/TabBadge.tsx, src/components/navigation/BottomNav.tsx</files>
  <action>
    Create TabBadge component (from RESEARCH.md pattern):

    ```typescript
    interface TabBadgeProps {
      count?: number;      // For number badges (Orders)
      showDot?: boolean;   // For red dot (Chat unread)
    }
    ```

    - If showDot is true, render 8x8px red dot positioned top-right
    - If count provided, render min-width 20px pill with number (cap at 99+)
    - Use absolute positioning relative to parent
    - Red-500 background, white text for count

    Update BottomNav.tsx:
    - Import TabBadge
    - Add badge config to tabs array: `badge?: { type: 'dot' | 'count', key: string }`
    - Chat tab: badge type 'dot' (for unread messages)
    - Orders tab: badge type 'count' (for order updates)
    - For now, hardcode badge visibility as false (will be wired to real data later)
    - Render TabBadge inside Link, positioned relative to icon
    - Maintain all existing 44x44px touch targets
  </action>
  <verify>
    - TabBadge.tsx exports TabBadge component
    - BottomNav.tsx imports and uses TabBadge
    - Temporarily set badge props to test visibility (dot on Chat, count=3 on Orders)
    - Visual check: badges appear correctly positioned
    - Revert to default hidden state after verification
  </verify>
  <done>
    TabBadge component exists and BottomNav supports conditional badge rendering for Chat (dot) and Orders (count).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Home tab with welcome message and camera CTA</name>
  <files>src/app/(tabs)/page.tsx</files>
  <action>
    Implement Home tab empty state per CONTEXT.md:
    - Brief welcome message explaining the app
    - Camera prompt as primary action

    Layout:
    - Centered content with proper thumb zone consideration
    - Welcome heading: "Welcome to North South"
    - Subheading: "AI-powered custom cabinetry for Sydney homes"
    - Brief explanation: "Take a photo of your space and we'll create styled design renders in seconds. Configure your exact requirements with live pricing."
    - Large camera icon (96x96px) in zinc-900 circle
    - Primary CTA button: "Take Photo" with Camera icon
    - Button should be full-width max-w-sm, min-h-14 (56px) for thumb zone
    - Secondary text below: "Or browse your gallery"

    The "Take Photo" button will be wired to camera capture in Plan 04.
    For now, make it a button that logs to console.
  </action>
  <verify>
    - Visit http://localhost:3000/
    - Welcome message displays
    - Camera icon and "Take Photo" button visible
    - Button has 56px height (thumb-friendly)
    - Layout is centered and readable on mobile viewport
  </verify>
  <done>
    Home tab shows welcome message explaining the app with prominent camera CTA in thumb zone.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Design and Orders tabs with empty states</name>
  <files>src/app/(tabs)/design/page.tsx, src/app/(tabs)/orders/page.tsx</files>
  <action>
    Design tab empty state per CONTEXT.md:
    - Illustration placeholder (use Palette icon in muted circle, 80x80px)
    - Heading: "No designs yet"
    - Message: "Take a photo to start creating."
    - Optional: Ghost button "Start Designing" that navigates to Home

    Orders tab empty state per CONTEXT.md:
    - Package icon in muted circle (80x80px)
    - Heading: "No orders yet"
    - Message: "Submit a design to get a quote."
    - Brief explanation of process below

    Both pages:
    - Centered vertically with proper padding
    - Muted color scheme (zinc-100 background for icon, zinc-400 icon color)
    - Text hierarchy: heading bold, message regular, extra info muted
    - Consistent with existing page structure
  </action>
  <verify>
    - Visit http://localhost:3000/design
    - "No designs yet" empty state displays with illustration
    - Visit http://localhost:3000/orders
    - "No orders yet" empty state displays
    - Both layouts centered and consistent
  </verify>
  <done>
    Design tab shows "No designs yet" with prompt. Orders tab shows "No orders yet" with guidance.
  </done>
</task>

</tasks>

<verification>
- All 4 tabs accessible via bottom navigation
- Home: Welcome message + camera CTA visible
- Design: Empty state with "No designs yet"
- Orders: Empty state with "No orders yet"
- Chat: Existing placeholder (will be updated in Plan 05)
- Badge system ready but badges hidden by default
- All buttons/touch targets minimum 44x44px
- `npm run build` succeeds
</verification>

<success_criteria>
- TabBadge component created and integrated
- BottomNav supports conditional badge rendering
- Home tab has welcoming first-run experience
- Design and Orders tabs have clear empty states
- All touch targets meet mobile accessibility standards
</success_criteria>

<output>
After completion, create `.planning/phases/02-mobile-ui-shell/02-02-SUMMARY.md`
</output>
